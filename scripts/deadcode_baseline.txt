# Begründete Ausnahmen für scripts/deadcode_gate.sh — Format wie der
# deadcode-Output, aber ohne Zeilen-/Spaltennummern (die driften).
# Jeder Block braucht eine Begründung. Aufräumen, sobald der Grund entfällt.

# Testinfrastruktur per Paket-Design: eigener SMTP-Server für Tests, weil die
# lokale .env auf den echten Schulserver zeigt (siehe Paketkommentar).
internal/smtptest/server.go: unreachable func: Starte
internal/smtptest/server.go: unreachable func: GeschlossenerPort
internal/smtptest/server.go: unreachable func: lausche
internal/smtptest/server.go: unreachable func: bediene
internal/smtptest/server.go: unreachable func: leseNachricht
internal/smtptest/server.go: unreachable func: adresseAus

# Episodischer Littera-Werkzeugkasten: Prüfwerkzeug für die noch ausstehende
# Littera-Migration (nächster Lauf nach frischem Littera-Backup). BarcodeInhalt
# entschlüsselt die alte Littera-Druckzeichenkette und dient als Gegenprobe zur
# Exemplarnummer, aus der die EAN-13 gerechnet wird — 61.520 von 61.520 stimmen
# überein. Der Lauf hängt an LITTERA_CSV_DIR und braucht die echten Altdaten,
# die nicht im Repo liegen (Rolle des Alt-Decoders in 1a3733d0 dokumentiert).
# Kein Drift-Risiko: es existiert kein paralleler Live-Pfad zu diesen Funktionen.
internal/littera/abgang.go: unreachable func: IstAbschlussklasse
internal/littera/barcode.go: unreachable func: BarcodeInhalt
internal/littera/barcode.go: unreachable func: entschluessele
internal/littera/barcode.go: unreachable func: entschluesseleZiffer
internal/littera/leser.go: unreachable func: NurArt

# Selbstdeklarierter Test-/Langlauf-Helfer (siehe Doc-Kommentar der Methode);
# wird von den pgtest-Support-Dateien mehrerer Pakete benutzt.
internal/uebernahme/protokoll.go: unreachable func: Protokoll.Leeren

# Erweiterungspunkt ohne Zuhörer — befristet, mit Ablaufdatum "nach dem Pilot".
#
# plugins/hooks.go bietet Register/Dispatch; fünf DispatchEvent-Aufrufe stehen live in
# den Rückgabe-, Ausleih- und Gerätepfaden (jeweils NACH dem Commit, neben dem
# Audit-Eintrag). Ohne registrierten Zuhörer kehrt DispatchEvent still zurück — die
# Aufrufe sind also inert, nicht tot.
#
# Warum RegisterHook trotzdem keinen Aufrufer hat: Der einzige war vorlage.Init(), das
# BEISPIEL-Plugin. Es lief bis zum 23.08.2026 in Produktion mit und schrieb bei jeder
# Rückgabe zwei Zeilen ins Log. Demo-Code gehört nicht in den Betrieb, also ist der
# Aufruf in main.go entfernt (dort steht die ausführliche Begründung).
#
# Warum nicht gleich alles löschen: Das wäre Kategorie C ("Refactoring ohne Anlass"),
# und die eigene Regel sagt dazu "nicht vor dem Pilotstart". Der Pilot läuft seit dem
# 23.08.2026. Chirurgie an den meistbenutzten Pfaden des Systems, um 128 Zeilen
# loszuwerden, die zur Laufzeit nichts kosten, ist der falsche Handel.
#
# NACH DEM PILOT ENTSCHEIDEN. Stand 23.08.2026 spricht alles fürs Löschen: genau EIN
# Ereignistyp, in 15 Monaten kein echtes Plugin, der Frontend-Teil (Extension.svelte)
# nie angeschlossen. Die fünf Stellen findet man dann über LogRueckgabe wieder.
plugins/hooks.go: unreachable func: RegisterHook
plugins/vorlage/vorlage.go: unreachable func: Init
