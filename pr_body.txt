🎯 **What:**
Added a missing unit test for the `NewCoverService` function in `internal/service/cover_service.go`. This function creates a new `CoverService` instance, but there was no test to ensure it correctly initializes the struct and assigns the dependencies.

📊 **Coverage:**
The new test covers:
- Initialization of a `CoverService` instance via `NewCoverService`.
- Passing the mocked `PgxPoolIface` database dependency.
- Verifying that the returned instance is not `nil`.
- Verifying that the assigned database pool matches the provided one.

✨ **Result:**
The test coverage for `cover_service.go` is improved by verifying the behavior of its public constructor. This acts as a safety net against accidental regressions where the dependency injection logic might get changed or broken, ensuring the application handles dependencies correctly.